spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://localhost:5432/questionBankNewDb
    username: postgres
    password: 1034
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
